version: "3"
services:
  ui:
    build: ./ui
    ports: ['3002:5173']
    volumes:
      - .:/workspace
  api:
    build: ./api
    environment:
      FLASK_DEBUG: 1
    ports: ['3001:8080']
    volumes:
      - .:/workspace