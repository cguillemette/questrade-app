version: "3"
services:
  ui:
    build: ./ui
    ports: ['6001:5173']
    volumes:
      - .:/workspace
  api:
    build: ./api
    environment:
      FLASK_DEBUG: 1
    ports: ['6002:8080']
    volumes:
      - .:/workspace